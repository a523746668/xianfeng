<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black_pinglun"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|center_vertical"
        android:layout_margin="5dip"
        android:layout_marginTop="50dp"
        android:background="@drawable/shape_kanshu_corners"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_popwindow_mulu"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/shape_all_corners"
            android:gravity="center_horizontal"
            android:text="目录"
            android:textColor="@drawable/white"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/tv_popwindow_shuqian"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center_horizontal"
            android:text="书签"
            android:textColor="@color/black"
            android:textSize="18sp"
            android:visibility="visible" />

        <TextView
            android:id="@+id/tv_popwindow_pizhu"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center_horizontal"
            android:text="批注"
            android:textColor="@color/black"
            android:textSize="18sp"
            android:visibility="gone" />
    </LinearLayout>

    <ListView
        android:id="@+id/mListView_mulu"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_marginTop="18dp"
        android:divider="#333333"
        android:dividerHeight="1px"/>

    <ListView
        android:id="@+id/mListView_book_mark"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_marginTop="18dp"
        android:divider="#333333"
        android:dividerHeight="1px"
        android:visibility="gone"/>

    <ListView
        android:id="@+id/mListView_book_remark"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_marginTop="18dp"
        android:divider="#333333"
        android:dividerHeight="1px"
        android:visibility="gone"/>

</LinearLayout>